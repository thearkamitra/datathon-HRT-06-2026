"""Tailored modeler for the Zurich Datathon 2026 (Method 2).

A Sharpe-aware boosted-tree pipeline on rich OHLC features. The module is
designed to be imported from a flat ``sys.path`` entry pointing directly at this
folder (the folder name ``tailored-modeler`` is not a valid Python identifier,
so the sub-modules are consumed as top-level names by callers).
"""

__version__ = "0.1.0"
